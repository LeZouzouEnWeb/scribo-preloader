function fadeInAndOut(t, e, n) { t.style.opacity = 0; let i = null; window.requestAnimationFrame((function o(d) { i || (i = d); let a = d - i; if (a < e) t.style.opacity = 0; else { if (!(a < e + n)) return void (t.style.opacity = 1); { let i = (a - e) / n; t.style.opacity = i } } window.requestAnimationFrame(o) })) } function fadeOutAndIn(t, e, n) { t.style.opacity = 1; let i = null; window.requestAnimationFrame((function o(d) { i || (i = d); let a = d - i; if (a < e) t.style.opacity = 1; else { if (!(a < e + n)) return void (t.style.opacity = 0); { let i = 1 - (a - e) / n; t.style.opacity = i } } window.requestAnimationFrame(o) })) } document.addEventListener("DOMContentLoaded", (function () { window.addEventListener("load", (function () { let t = document.getElementById("chgt"); fadeOutAndIn(t, 500, 1e3), window.setTimeout((function () { t.innerHTML = "" }), 3e3) })) }));